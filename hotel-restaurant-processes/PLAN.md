# PLAN.md — Автоматизация отчёта ресторана «Монблан»
> Обновляется по мере выполнения задач.

---

## Общий прогресс

| Этап | Название | Статус |
|---|---|---|
| 1 | Подготовка и доступы | ✅ Завершён |
| 2 | iikoWeb OLAP — подключение и тест | ✅ Завершён |
| 3 | Google Sheets — структура таблицы | ✅ Завершён |
| 4 | Python-скрипт — автосбор данных | ✅ Завершён |
| 5 | GitHub Actions — автозапуск | ✅ Завершён |
| 6 | MAX мессенджер — отчёты | 🔄 Код готов, ждём секреты |
| 7 | Финальный тест и запуск в бой | 🔄 В процессе |

---

## Этап 1 — Подготовка и доступы
**Статус: ✅ Завершён**

- [x] Описать текущий процесс, провести анализ бизнес-процессов
- [x] Определить архитектуру, создать CLAUDE.md и PLAN.md
- [x] Уточнить кол-во столов и мест (15 / 90 с 15.12.2025; до — 14 / 58)
- [x] Получить доступ к iikoWeb: `buh` / `Vjy,kfy2024` (storeId=82455)
- [x] Добавить в GitHub Secrets: `GOOGLE_SHEETS_ID`, `GOOGLE_SERVICE_ACCOUNT_JSON`
- [ ] Получить MAX токены: `MAX_BOT_TOKEN`, `MAX_OWNER_USER_ID`, `MAX_DEV_USER_ID` — **ждём от клиента**

---

## Этап 2 — iikoWeb OLAP: подключение и тест
**Статус: ✅ Завершён**

**Итог:** iiko Transport API не даёт POS-данные. Рабочее решение — iikoWeb OLAP API.

- [x] Исследовать iiko Transport API → не работает для POS-данных (всегда 0 заказов)
- [x] Найти правильный формат OLAP: `olapType`, `groupFields`, `dataFields`
- [x] Авторизация: `POST /api/auth/login {login, password}` → JWT (TTL 1200 сек)
- [x] Реализовать `IikoWebSession` с auto-refresh токена + SSL retry
- [x] Реализовать `_olap_query()` с правильными именами полей
- [x] Подтвердить реальными данными: 2026-04-05: **34 900 руб.**, 20 чеков, 25 гостей
- [x] Разбивка по категориям Кухня/Бар через поле `DishCategory` + keyword-матч для позиций без категории
- [x] Временные срезы (утро/день/вечер) через `groupFields: ["HourClose"]`
- [x] Отмены через отдельный запрос с `OrderDeleted: ["DELETED"]`
- [x] ✅ Критерий выполнен: реальные данные получены и корректно разбиты

---

## Этап 3 — Google Sheets: структура таблицы
**Статус: ✅ Завершён**

- [x] Создать Google Таблицу (`1Wcvn2mJFgOfcdm3mUQpYLoU92H3_bhGUJA_NnBwbDNI`)
- [x] Сервисный аккаунт (`montblanc-report@composite-wind-449411-d8.iam.gserviceaccount.com`)
- [x] `sheets_writer.py`: транспонированная структура (A — параметры, строка 1 — даты)
- [x] Лист «Ежедневно»: строки 2–16 авто, строки 17–25 ручной ввод (не перезаписываются)
- [x] Лист «Еженедельно»: строка 1 — номер недели, строка 2 — период
- [x] `_find_or_create_date_column()`: поиск по дате, обработка Excel serial numbers
- [x] Дата пишется с `valueInputOption="RAW"` (не превращается в серийный номер)
- [x] `_aggregate_weekly()` корректно работает с транспонированной структурой
- [x] ✅ Критерий: данные за 30.03–05.04.2026 записаны корректно, 7 колонок

**Текущая структура листа «Ежедневно» (строки):**
```
1: Дата | 2: День недели | 3: Выручка итого | 4: Кол-во чеков | 5: Средний чек
6: Гости | 7: Кухня | 8: Бар | 9: Утро—выручка | 10: Утро—гости
11: День—выручка | 12: День—гости | 13: Вечер—выручка | 14: Вечер—гости
15: Отмены | 16: Списания | 17: Инкассация | 18: Расход | 19: Остаток нал
20: Повара кол-во | 21: Официанты кол-во | 22: Бармены кол-во
23: Посудомойщицы кол-во | 24: Персонал итого | 25: Завтраки
```

---

## Этап 4 — Python-скрипт: автосбор данных
**Статус: ✅ Завершён**

- [x] `config.py` — константы и дефолты для всех env vars
- [x] `utils.py` — `yesterday_utc5()`, `fmt_money()`, `parse_admin_reply()`
- [x] `iiko_client.py` — `IikoWebSession`, `collect_daily_data_iiko_web()`
- [x] `sheets_writer.py` — `write_daily_row()` пишет только строки 1–16
- [x] `main.py` — три режима: `collect` / `report` / `weekly`
  - `collect`: iiko OLAP → Google Sheets строки 1–16
  - `report`: читает Sheets (авто + ручной ввод) → отчёт в MAX
  - `weekly`: агрегация листа «Ежедневно» → лист «Еженедельно»
- [x] ✅ Критерий: скрипт корректно собирает и пишет данные

---

## Этап 5 — GitHub Actions: автозапуск
**Статус: ✅ Завершён**

- [x] `daily_report.yml`: cron `30 18 * * *` + резервный `30 1 * * *` + workflow_dispatch
- [x] `weekly_report.yml`: cron `0 2 * * 1` + workflow_dispatch
- [x] Все Google Sheets Secrets добавлены
- [x] iiko-дефолты захардкожены в workflow (IIKO_STORE_ID=82455)
- [x] SSL retry решён: `verify=False` + `Retry(total=3, backoff_factor=2)`
- [ ] Добавить MAX Secrets после получения от клиента

---

## Этап 6 — MAX мессенджер: отчёты
**Статус: 🔄 Код готов, ждём секреты от клиента**

- [x] `max_bot.py` — MaxBot: `send_message()`, `poll_for_reply()`, `flush_updates()`
- [x] `_build_owner_report()` — формирует отчёт с авто + ручными данными
- [x] `_alert_dev()` — алерты разработчику при ошибках
- [ ] Добавить в GitHub Secrets: `MAX_BOT_TOKEN`, `MAX_OWNER_USER_ID`, `MAX_DEV_USER_ID`
- [ ] ✅ Финальный критерий: отчёт приходит собственнику каждое утро

---

## Этап 7 — Финальный тест и запуск в бой
**Статус: 🔄 В процессе**

**Что сделано:**
- [x] iikoWeb OLAP работает с реальными данными (30.03–05.04.2026)
- [x] GitHub Actions `collect` запускается по расписанию
- [x] Данные за 30.03–05.04 записаны в Google Sheets корректно
- [x] Структура листа соответствует техническому заданию
- [x] Ручной ввод администратора не перезаписывается ночным скриптом

**Что осталось:**
- [ ] Получить MAX токены от клиента → добавить в GitHub Secrets
- [ ] Запустить `main.py report` — проверить что отчёт корректно формируется
- [ ] Обучить администратора: заполнять строки 17–25 в Google Sheets каждый день
- [ ] Первый автоматический ежедневный отчёт отправлен собственнику
- [ ] ✅ Критерий: система работает без вмешательства 7 дней подряд

---

---

## Этап 8 — Дашборд сравнения недель (Google Apps Script)
**Статус: ✅ Завершён**

Отдельный лист **«Дашборд»** (GID=1669207980) в таблице iiko.

- [x] Лист «Дашборд» создан через `setup_dashboard.py`
- [x] 12 KPI: выручка, кухня, бар, гости, чеки, средний чёк, временные срезы, оборачиваемость
- [x] Сравнение недель 1–5: 2025 vs 2026 (данные из скрытых строк 50–73)
- [x] Цветовая индикация: 🔴 < −10% | 🟡 −3%..−10% | ⚪ норма | 🟢 > +5%
- [x] Блок «Сигналы недели» — автоматически перечисляет проблемные и растущие метрики
- [x] Выбор недели в B2 (выпадающий список 1–5)
- [x] AI-анализ через Groq API (Llama 3.3 70B, бесплатно, без карты)
  - БЛОК 1: Возможные причины проблем (строки 34–36)
  - БЛОК 2: Рекомендации (строки 38–40)
  - БЛОК 3: Управленческие выводы (строки 42–44)
- [x] GAS-файл: `monblan-gas/monblan_dashboard.gs`
- [x] Script Property: `GROQ_API_KEY` (ключ с console.groq.com)

**Формулы (ru_RU локаль — разделитель «;»):**
- `=INDEX($B$50:$F$50;1;$B$2)` — значение 2025 по выбранной неделе
- `=IFERROR(IF(B6=0;"—";(C6-B6)/B6);"—")` — дельта %
- Условное форматирование: `=$D6<-10%`, `=$D6<-3%`, `=$D6>5%`

---

## Следующий шаг

1. **От клиента:** MAX токены (бот + 2 user_id: собственник + разработчик)
2. **После получения:** добавить в GitHub Secrets → проверить отправку отчёта в MAX

---

## Легенда

| Статус | Значение |
|---|---|
| ✅ Завершён | Все задачи выполнены, критерий приёмки пройден |
| 🔄 В процессе | Часть задач выполнена |
| ⬜ Ожидает | Не начат или ждёт внешних данных |

---

*Обновлено: 21 апреля 2026*
