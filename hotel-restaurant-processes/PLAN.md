# PLAN.md — Автоматизация отчёта ресторана «Монблан»
> Обновляется автоматически по мере выполнения задач.

---

## Общий прогресс

| Этап | Название | Статус |
|---|---|---|
| 1 | Подготовка и доступы | ✅ Завершён |
| 2 | iikoWeb OLAP — подключение и тест | ✅ Завершён |
| 3 | Google Sheets — структура таблицы | ✅ Завершён |
| 4 | Python-скрипт — автосбор данных | ✅ Завершён |
| 5 | MAX мессенджер — отчёты и ручной ввод | 🔄 Код готов, ждём секреты |
| 6 | GitHub Actions — автозапуск | ✅ Завершён |
| 7 | Финальный тест и запуск в бой | 🔄 В процессе |

---

## Этап 1 — Подготовка и доступы
**Статус: ✅ Завершён**

- [x] Описать текущий процесс, провести анализ бизнес-процессов
- [x] Определить архитектуру, создать CLAUDE.md и PLAN.md
- [x] Уточнить кол-во столов и мест (15 / 90 с 15.12.2025; до — 14 / 58)
- [x] Получить доступ к iikoWeb: `buh` / `Vjy,kfy2024` (storeId=82455)
- [x] Добавить в GitHub Secrets: `IIKO_WEB_LOGIN`, `IIKO_WEB_PASSWORD`, `IIKO_STORE_ID`
- [x] Добавить в GitHub Secrets: `GOOGLE_SHEETS_ID`, `GOOGLE_SERVICE_ACCOUNT_JSON`
- [ ] Получить MAX токены: `MAX_BOT_TOKEN`, `MAX_OWNER_USER_ID`, `MAX_ADMIN_USER_ID`, `MAX_DEV_USER_ID` — **ждём от клиента**

---

## Этап 2 — iikoWeb OLAP: подключение и тест
**Статус: ✅ Завершён**

**Итог:** iiko Transport API (`api-ru.iiko.services`) не даёт POS-данные.
Рабочее решение — iikoWeb OLAP API (`kafe-monblan.iikoweb.ru`).

- [x] Исследовать iiko Transport API → не работает для POS-данных (всегда 0 заказов)
- [x] Исследовать iikoWeb API через JS-бандл (reverse-engineering 726KB + 218KB)
- [x] Найти правильный формат OLAP: `olapType`, `groupFields`, `dataFields` (не `reportType` и т.д.)
- [x] Найти статус ответа: `SUCCESS` (не `READY` как в документации)
- [x] Авторизация: `POST /api/auth/login {login, password}` → JWT (TTL 1200 сек)
- [x] Реализовать `IikoWebSession` с auto-refresh токена
- [x] Реализовать `_olap_query()` с правильными именами полей
- [x] Подтвердить реальными данными:
  - 2026-04-05: **34 900 руб.**, 20 чеков, 25 гостей
  - 2024-01-01: 213 305 руб., 365 чеков, 445 гостей
- [x] Создать `test_iiko.py` — автопоиск даты с данными за 30 дней
- [x] ✅ Критерий выполнен: реальные данные получены

**Оставшийся вопрос:**
- [ ] Уточнить названия `DishCategory.Accounting` для разбивки Кухня/Бар (сейчас всё в "Другое")

---

## Этап 3 — Google Sheets: структура таблицы
**Статус: ✅ Завершён**

- [x] Создать Google Таблицу (`1Wcvn2mJFgOfcdm3mUQpYLoU92H3_bhGUJA_NnBwbDNI`)
- [x] Создать сервисный аккаунт (`montblanc-report@composite-wind-449411-d8.iam.gserviceaccount.com`)
- [x] Создать `sheets_writer.py` с транспонированной структурой
- [x] Лист «Ежедневно»: A — параметры, строка 1 — даты
- [x] Лист «Еженедельно»: строка 1 — номер недели, строка 2 — период
- [x] `test_sheets.py` — проверка подключения и записи
- [x] ✅ Критерий: тестовые данные записываются корректно

---

## Этап 4 — Python-скрипт: автосбор данных
**Статус: ✅ Завершён**

- [x] `config.py` — константы: `IIKO_WEB_*`, `SHEETS_*`, `MAX_*`, `get_capacity()`
- [x] `utils.py` — `retry()`, `fmt_money()`, `yesterday_utc5()`, `parse_admin_reply()`
- [x] `main.py` — точка входа `daily()` / `weekly()`
- [x] `daily()`: iikoWeb OLAP → Sheets → MAX запрос → polling → Sheets → отчёт
- [x] `weekly()`: агрегация листа «Ежедневно» → «Еженедельно» → дайджест
- [x] `collect_daily_data(report_date)` — новая сигнатура без `token`/`org_id`
- [x] ✅ Критерий: скрипт запускается без синтаксических ошибок

---

## Этап 5 — MAX мессенджер: отчёты и ручной ввод
**Статус: 🔄 Код готов, ждём секреты от клиента**

- [x] `max_bot.py` — MaxBot: `send_message()`, `poll_for_reply()`, `flush_updates()`
- [x] Интеграция в `daily()` — polling 30 мин
- [x] Запрос ручных данных у администратора (касса, персонал, завтраки)
- [x] Парсинг ответа → запись в Sheets
- [x] Алерты разработчику (`_alert_dev`)
- [ ] Добавить в GitHub Secrets: `MAX_BOT_TOKEN`, `MAX_OWNER_USER_ID`, `MAX_ADMIN_USER_ID`, `MAX_DEV_USER_ID`
- [ ] ✅ Финальный критерий: полный диалог администратор → бот → Sheets работает

---

## Этап 6 — GitHub Actions: автозапуск
**Статус: ✅ Завершён**

- [x] `daily_report.yml`: cron `30 18 * * *` + резервный `30 1 * * *` + workflow_dispatch
- [x] `weekly_report.yml`: cron `0 2 * * 1` + workflow_dispatch
- [x] `timeout-minutes: 60` для daily (30 мин на ожидание администратора)
- [x] Все iikoWeb и Google Sheets Secrets добавлены
- [ ] Добавить MAX Secrets после получения от клиента

---

## Этап 7 — Финальный тест и запуск в бой
**Статус: 🔄 В процессе**

**Что сделано:**
- [x] iikoWeb OLAP работает с реальными данными
- [x] `test_iiko.py` проходит успешно (34 900 руб. за 05.04.2026)

**Что осталось:**
- [ ] Уточнить названия категорий Кухня/Бар (`DishCategory.Accounting`) в iikoWeb
- [ ] Получить MAX токены от клиента → добавить в GitHub Secrets
- [ ] Прогнать `main.py` вручную на данных за несколько дней
- [ ] Сверить результаты с реальными данными ресторана
- [ ] Первый автоматический ежедневный отчёт отправлен собственнику
- [ ] Администратор обучен отвечать боту
- [ ] ✅ Критерий: система работает без вмешательства 7 дней подряд

---

## Следующий шаг

1. **От клиента:** MAX токены (бот + 3 user_id)
2. **Технически:** уточнить названия бухгалтерских категорий Кухня/Бар через iikoWeb

---

## Легенда

| Статус | Значение |
|---|---|
| ✅ Завершён | Все задачи выполнены, критерий приёмки пройден |
| 🔄 В процессе | Часть задач выполнена |
| ⬜ Ожидает | Не начат или ждёт внешних данных |

---

*Обновлено: 14 апреля 2026 — iikoWeb OLAP подтверждён на реальных данных*
