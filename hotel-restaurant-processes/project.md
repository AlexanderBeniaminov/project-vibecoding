# project.md — Автоматизация отчёта ресторана «Монблан»
> Обзорный документ проекта. Технические детали — в [CLAUDE.md](CLAUDE.md). Статус этапов — в [PLAN.md](PLAN.md).

---

## Контекст

**Объект:** Ресторан «Монблан», курорт Губаха (Пермский край)
**Система учёта:** iikoServer (локальный сервер, документация ru.iiko.help)

**Вместимость зала:**
| Период | Столов | Мест |
|---|---|---|
| до 15.12.2025 | 14 | 58 |
| с 15.12.2025 | 15 | 90 |

> При сравнении периодов (YoY) учитывать изменение вместимости — оборачиваемость считать отдельно для каждого периода.

**Клиент:** Собственник курорта Губаха

**Проблема:** Администратор вручную переносит данные из iiko в Excel каждый день.
Это занимает **28–41 мин/день** и **60–90 мин/нед** — итого ~15 часов в месяц.
Плюс человеческий фактор: ошибки, пропуски, несвоевременное заполнение.

---

## Цель

Автоматически собирать данные из iiko каждый вечер, записывать в Google Sheets и отправлять готовый отчёт собственнику в Telegram. Ручной ввод оставить только для данных, которых нет в iiko (касса, персонал).

---

## Что автоматизируется

**Из iiko API (автоматически):**
выручка по типам оплаты, кухня/бар, кол-во чеков и гостей, средний чек, временные срезы (утро/день/вечер), топ блюд, отмены, списания, анализ групп гостей, градация чеков.

**Ручной ввод через Telegram-бот (остаётся):**
касса (инкассация, расход, остаток), персонал (кол-во и з/п по должностям), завтраки.

---

## Архитектура

```
iiko API (23:30 ежедневно, UTC+5)
   │
   ▼
Python-скрипт (GitHub Actions)
   │
   ├──► Google Sheets (ID: 1Wcvn2mJFgOfcdm3mUQpYLoU92H3_bhGUJA_NnBwbDNI)
   │         ├── Лист «Ежедневно»   — параметры в строках, дата в колонке
   │         ├── Лист «Еженедельно» — параметры в строках, неделя в колонке
   │         └── Лист «Дашборд»     — графики (позже)
   │
   └──► Telegram-бот
             ├── Собственнику → ежедневный отчёт
             ├── Собственнику → еженедельный дайджест (пн 07:00)
             └── Администратору → запрос ручных данных (касса, персонал)
```

**Стоимость:** 0 руб./месяц (все компоненты бесплатны)

---

## Файлы папки

| Файл | Назначение |
|---|---|
| [process-restoran.md](process-restoran.md) | Детальный разбор текущего процесса (шаги, данные, источники, точки отказа) |
| [business-analysis.md](business-analysis.md) | Схема бизнес-процессов отеля и ресторана, приоритеты автоматизации |
| [business-schema.html](business-schema.html) | Визуальная схема (открыть в браузере) |
| [CLAUDE.md](CLAUDE.md) | Технический контекст для разработки (iiko API, структура кода, логика) |
| [PLAN.md](PLAN.md) | 7 этапов внедрения с текущим статусом |

## Скрипты (`scripts/`)

| Файл | Назначение |
|---|---|
| `main.py` | Точка входа: `python3 main.py daily` / `weekly` |
| `iiko_client.py` | Клиент iiko REST API v2 (auth, все запросы) |
| `sheets_writer.py` | Запись в Google Sheets (транспонированная структура) |
| `utils.py` | Утилиты: дата UTC+5, retry, форматирование, парсинг ответа админа |
| `config.py` | Константы (без секретов) |
| `test_iiko.py` | Быстрая проверка подключения к iiko |
| `test_sheets.py` | Быстрая проверка подключения к Google Sheets |

---

## Текущий статус (30 марта 2026)

| Компонент | Статус |
|---|---|
| iiko API — все 9 запросов | ✅ Работает (демо-стенд) |
| Google Sheets — структура и запись | ✅ Работает |
| GitHub Actions — расписание | ✅ Работает, первый запуск прошёл |
| GitHub Secrets | ✅ Добавлены (iiko + Google) |
| Telegram-бот | ⬜ Ожидает токена от клиента |
| Боевой iiko-сервер | ⬜ Ожидает доступа от клиента |

---

## Что нужно от клиента для завершения

1. **Боевой iiko** — адрес сервера + логин + пароль (для реальных данных)
2. **Telegram** — создать бота через @BotFather, прислать токен; прислать свой chat_id и chat_id администратора
3. **Мероприятия** — как помечаются в iiko: тег или отдельный тип заказа?

---

*Обновлено: 30 марта 2026*
