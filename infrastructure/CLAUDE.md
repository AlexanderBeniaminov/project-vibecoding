# Инструкции для Claude — Проект "Сервер + VPN"

## Кто пользователь
- Александр, Россия, Mac M2, VS Code
- Полный новичок в Linux/SSH/DevOps
- Два аккаунта: alex (Александр) и oleg (Олег)

## Правила работы
- Всегда давать готовые команды для copy-paste — без сокращений
- Не объяснять теорию, если не спрашивают
- Сначала делать, потом объяснять по запросу
- Всегда указывать: эту команду на Маке или на сервере
- Перед началом сессии читать PLAN.md и STATUS.md

## Сервер
- IP: 84.54.30.209
- ОС: Ubuntu 24.04.4, 2GB RAM
- Логин: root (через SSH) / parser (для парсеров)
- WireGuard: wg-quick@wg0, порт 51820/UDP
- VPN-подсеть: 10.66.66.1 (сервер), 10.66.66.2 (alex), 10.66.66.3 (oleg)
- Python: /home/parser/venv/bin/python (Python 3.12)

## Структура файлов на сервере
```
/home/parser/
├── venv/                    # Python virtualenv
├── config/
│   ├── alex/
│   │   ├── settings.py      # ❌ не заполнен
│   │   └── service_account.json  # ❌ не загружен
│   └── oleg/
│       ├── settings.py      # ❌ не заполнен
│       └── service_account.json  # ❌ не загружен
├── parsers/                 # скрипты парсеров
├── bots/                    # Telegram и VK боты
├── logs/                    # логи запусков
└── data/                    # временные данные
```

## Важные детали
- service_account.json уже существует в Google Cloud (не создавать заново)
- iiko-скрипты уже написаны локально на Mac — нужно перенести на сервер
- Существующий VPN пользователя (не наш) — оставить работать, не конфликтовать
- split-tunnel: зарубежный трафик через наш VPN, российский — напрямую
